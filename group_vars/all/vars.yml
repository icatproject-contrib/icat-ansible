---
# Payara user and admin user's password
payara_user: "glassfish"
payara_admin_password: "{{ vault_payara_admin_password }}"

# Database root user's username and password
db_root_username: "{{ vault_db_root_username }}"
db_root_password: "{{ vault_db_root_password }}"

# Database user's username and password (not db/root)
db_user_username: "{{ vault_db_user_username }}"
db_user_password: "{{ vault_db_user_password }}"

# ICAT database hostname, database name, username and password
db_icat_hostname: "localhost"
db_icat_mariadb_port: 3306
db_icat_oracle_port: 1521
icat_database: "{{ vault_icat_database }}"
db_icat_username: "{{ vault_db_icat_username }}"
db_icat_password: "{{ vault_db_icat_password }}"

# Authn_simple usernames
authn_root_username: "{{ vault_authn_root_username }}"
authn_ingest_username: "{{ vault_authn_ingest_username }}"
authn_reader_username: "{{ vault_authn_reader_username }}"
authn_icatuser_username: "{{ vault_authn_icatuser_username }}"

# Authn_simple passwords
authn_root_password: "{{ vault_authn_root_password }}"
authn_ingest_password: "{{ vault_authn_ingest_password }}"
authn_reader_password: "{{ vault_authn_reader_password }}"
authn_icatuser_password: "{{ vault_authn_icatuser_password }}"

# Authn_simple server URL
authn_simple_url: "{{ ansible_fqdn }}"

# Authn_db server URL
authn_db_url: "{{ ansible_fqdn }}"

# Authn_anon server URL
authn_anon_url: "{{ ansible_fqdn }}"

# Authn_ldap server URL
authn_ldap_url: "{{ ansible_fqdn }}"

# ICAT Lucene server URL
lucene_url: "{{ ansible_fqdn }}"

# ICAT server URL
icat_url: "{{ ansible_fqdn }}"

# IDS server URL
ids_url: "{{ ansible_fqdn }}"

# TopCat server URL
topcat_url: "{{ ansible_fqdn }}"

# TopCat database hostname, database name, username and password
db_topcat_hostname: "localhost"
topcat_database: "{{ vault_topcat_database }}"
db_topcat_username: "{{ vault_db_topcat_username }}"
db_topcat_password: "{{ vault_db_topcat_password }}"

# Snapshot download URLs
#authn_anon_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/authn.anon/3.0.0-SNAPSHOT/authn.anon-3.0.0-20220512.125031-1-distro.zip"
#authn_db_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/authn.db/3.0.0-SNAPSHOT/authn.db-3.0.0-20220512.125133-1-distro.zip"
#authn_ldap_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/authn.ldap/3.0.0-SNAPSHOT/authn.ldap-3.0.0-20220512.125153-1-distro.zip"
#authn_simple_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/authn.simple/3.0.0-SNAPSHOT/authn.simple-3.0.0-20220512.125327-1-distro.zip"
#icat_lucene_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/icat.lucene/2.0.0-SNAPSHOT/icat.lucene-2.0.0-20220512.124921-1-distro.zip"
#icat_server_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/icat.server/6.0.0-SNAPSHOT/icat.server-6.0.0-20220512.124604-1-distro.zip"
#ids_server_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/ids.server/1.11.1-SNAPSHOT/ids.server-1.11.1-20200724.085739-1-distro.zip"
#ids_storage_file_snapshot_url: ""
#topcat_snapshot_url: "https://repo.icatproject.org/repo/org/icatproject/topcat/2.4.9-SNAPSHOT/topcat-2.4.9-20220427.112840-1-distro.zip"
